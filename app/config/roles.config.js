const db = require("../models");
const Role = db.roles;

function initial() {
    Role.create({
      id: 1,
      name: "user"
    });
   
    Role.create({
      id: 2,
      name: "moderator"
    });
   
    Role.create({
      id: 3,
      name: "admin"
    });
}

const initialize = {initial}

module.exports = initialize;